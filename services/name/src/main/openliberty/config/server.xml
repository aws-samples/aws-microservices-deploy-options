<server description="Names service">

  <featureManager>
    <feature>jaxrs-2.0</feature>
  </featureManager>

  <httpEndpoint httpPort="${default.http.port}" id="defaultHttpEndpoint" host="*" />

  <webApplication location="name.war" contextRoot="/" />

</server>
